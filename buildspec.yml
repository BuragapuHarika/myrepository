version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.12
    commands:
      - echo Installing dependencies...
      - pip install flask

  build:
    commands:
      - echo Build started...
      - echo Build completed successfully.

artifacts:
  files:
    - app.py
